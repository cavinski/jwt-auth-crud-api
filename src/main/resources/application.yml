spring:
  application:
    name: auth-crud-api
  
  datasource:
    url: jdbc:postgresql://postgres:5432/api_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
